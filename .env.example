# Docker 및 백엔드 설정 환경 변수 예시 (뼈대)
# 이 파일은 템플릿 역할(.env.example)이므로 Git에 포함됩니다.
# 실제 사용할 때는 복사하여 .env 로 이름을 변경 후 사용하세요.

DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_secure_password
DB_NAME=stitch_db