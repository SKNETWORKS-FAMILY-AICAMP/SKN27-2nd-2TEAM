# 16. 파트별 작업 세부사항 (최종 수정안)

본 프로젝트는 아래 3개 파트로 구분하여 진행한다.

1. 프론트/UI 구현
2. 백엔드 구현 (MySQL → CSV 파이프라인)
3. 머신러닝 모델 학습 및 튜닝

---

## 16.1 프론트/UI 구현 (시각화 + 추론 실행)

### 목표

프론트는 단순 UI가 아니라,
**CSV 데이터를 기반으로 전처리 + 시뮬레이션 + 모델 예측까지 수행하는 실행 레이어** 역할을 한다.

---

### 역할 범위 (확정)

* CSV 파일 로드
* 시뮬레이션 데이터 변조
* 머신러닝 전처리 함수 실행
* 모델 로드 및 예측 수행
* 결과 시각화
* 사용자 인터랙션 처리

---

## 🔥 핵심 역할

프론트는 아래 3가지를 수행한다:

1. 데이터 로드 (CSV)
2. 데이터 변조 (Simulation)
3. 전처리 + 모델 추론

---

## 데이터 처리 흐름

```plaintext id="8g9zqa"
CSV 로드
  ↓
DataFrame 생성
  ↓
original_df / sim_df 분리
  ↓
sim_df에 시뮬레이션 적용
  ↓
전처리 함수 적용 (ML 제공)
  ↓
모델 예측 수행
  ↓
결과 비교 및 시각화
```

---

## 세부 작업 항목

### 1) CSV 로드

```python id="7gr8bw"
df = pd.read_csv("netflix_segment_data.csv")
```

---

### 2) 데이터 복사

```python id="rj5z9g"
original_df = df.copy()
sim_df = df.copy()
```

---

### 3) 시뮬레이션 데이터 변조

```python id="8x6syo"
sim_df['subscription_plan'] = 'Standard'
sim_df['monthly_revenue'] = sim_df['monthly_revenue'] * 0.9
```

---

### 4) 전처리 함수 적용 (ML 제공)

```python id="s8yjxk"
X_original = preprocess(original_df)
X_sim = preprocess(sim_df)
```

---

### 5) 모델 예측

```python id="d9w2hl"
pred_original = model.predict_proba(X_original)[:, 1]
pred_sim = model.predict_proba(X_sim)[:, 1]
```

---

### 6) 결과 비교 및 시각화

* KPI 카드
* 분포 비교
* 변화량 분석

---

## UI 구성

* Dashboard
* 모델 설명 페이지
* 시뮬레이션 페이지

---

## 프론트 특징

* 데이터 처리 + 추론까지 담당
* 백엔드 의존 최소화
* 빠른 반복 개발 가능

---

## 16.2 백엔드 구현 (MySQL → CSV 파이프라인)

### 목표

DB에서 고객 데이터를 조회하여
**CSV 파일 형태로 전달하는 단방향 데이터 공급 역할 수행**

---

## 역할 범위

* 고객 데이터 조회
* 세그먼트 정의
* SQL 실행
* CSV 생성 및 전달

---

## 데이터 흐름

```plaintext id="p3j9sd"
MySQL → SQL → DataFrame → CSV → 프론트 전달
```

---

## 세부 작업

### 1) 세그먼트 정의

* 프론트 Dropdown과 매핑

---

### 2) SQL 쿼리 작성

---

### 3) CSV 생성

```python id="3d3qbt"
df.to_csv("netflix_segment_output.csv", index=False)
```

---

### 4) 데이터 전달

* 파일 다운로드
  또는
* 파일 경로 제공

---

## 특징

* 단방향 구조
* 상태 없음
* 단순 데이터 공급

---

## 제외 범위

* 모델 실행
* 전처리
* 시뮬레이션

---

## 16.3 머신러닝 모델 학습 및 튜닝 (핵심 확장)

### 목표

모델을 학습하는 것뿐만 아니라,
**프론트에서 재사용 가능한 전처리 및 추론 구조를 제공**한다.

---

## 🔥 핵심 역할

머신러닝 파트는 아래 3가지를 반드시 제공해야 한다:

1. 학습된 모델
2. 전처리 함수 (재사용 가능)
3. 실험 기록 (리포트)

---

## 1) 전처리 함수 제공 (중요)

프론트에서 동일하게 사용할 수 있도록
전처리 과정을 함수화하여 제공한다.

---

### 형태 예시

```python id="1a9l3x"
def preprocess(df):
    # 결측치 처리
    # 인코딩
    # 스케일링
    # 컬럼 정렬
    return X
```

---

## 요구사항

* 학습 시 사용한 로직과 100% 동일해야 함
* 입력 컬럼 순서 고정
* 누락 컬럼 처리 가능해야 함
* inference 전용으로 동작해야 함

---

## 2) 모델 저장

```python id="dsk82j"
joblib.dump(model, "model.pkl")
```

---

또는 pipeline 형태:

```python id="4px9k3"
pipeline = Pipeline([...])
joblib.dump(pipeline, "pipeline.pkl")
```

---

## 3) 실험 기록 관리 (강화)

### 목적

* 모델 선택 근거 확보
* 프론트 설명 페이지 활용
* 재현성 확보

---

### 기록 항목

* 실험 ID
* 모델 종류
* 파라미터
* feature 목록
* 전처리 방식
* 성능 지표
* 결과 해석

---

### 기록 방식 (추천)

#### Markdown 리포트

* 실험별 문서 생성
* 그래프 포함
* 팀 공유 가능

---

##
